<div class="container my-4">
  <div class="card">
    <div class="card-header bg-success text-white">
      <h1 class="h4 mb-0">Payment Successful!</h1>
    </div>
    <div class="card-body">
      <div class="text-center mb-4">
        <i class="bi bi-check-circle-fill text-success" style="font-size: 4rem;"></i>
      </div>
      
      <h2 class="h5 mb-3">Thank you for your order!</h2>
      <p>Your payment has been processed successfully. We've updated your order status to paid.</p>
      
      <div class="alert alert-info">
        <p class="mb-0"><strong>Order ID:</strong> #<%= @order.id %></p>
        <p class="mb-0"><strong>Payment Reference:</strong> <%= @order.stripe_payment_id %></p>
      </div>
      
      <p>You'll receive a confirmation email shortly with your order details. Your items will be processed for shipping soon.</p>
      
      <div class="mt-4">
        <%= link_to "View Order Details", order_path(@order), class: "btn btn-primary me-2" %>
        <%= link_to "Continue Shopping", products_path, class: "btn btn-outline-secondary" %>
      </div>
    </div>
  </div>
</div>